- title "Membership"

%p
  %strong member:
  =h @membership.member
%p
  %strong Group:
  =h @membership.group
%p
  %strong Archived At:
  =h @membership.archived_at

%p
  = link_to "Edit", edit_membership_path(@membership)
  |
  = link_to "Destroy", @membership, :confirm => 'Are you sure?', :method => :delete
  |
  = link_to "View All", memberships_path
